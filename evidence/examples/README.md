# Evidence examples

`not-run-manifest.json` 是誠實的空狀態範例。它用來教讀者辨認
`unknown`，不聲稱任何 command、commit 或 verifier 已執行。

真實 Evidence 必須由隔離、已提交 baseline 的 lab 執行
`scripts/collect-evidence.sh` 產生；不要手動把範例狀態改成 pass。
